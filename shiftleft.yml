source:
  branch: master
build_rules:
  - id: build-rule-identifier
    finding_types:
      - vuln
    severity:
      - critical
    # type:
    #   - SQL Injection
    # owasp_category:
    #   - a1-injection
    threshold: 0